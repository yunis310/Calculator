import React from 'react';

interface Props {
    id?: string;
}

const Result: React.FC<Props> = () => {
    return (
        <div>
            <p>result</p>
        </div>
    );
};

export default Result;
