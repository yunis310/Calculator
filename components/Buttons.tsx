import React from 'react';

interface Props {
    id?: string;
}

const Buttons: React.FC<Props> = () => {
    return (
        <div>
            <p>Buttons</p>
        </div>
    );
};

export default Buttons;
